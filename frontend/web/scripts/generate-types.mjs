#!/usr/bin/env node
// generate-types.mjs — placeholder for openapi-typescript codegen.
// Wiring deferred until the DRF schema endpoint exists (see Stream C of the
// Lattice vertical-slice plan).

console.log(
  'openapi-typescript generation not wired yet — see Stream C / DRF schema endpoint.'
);
process.exit(0);
