"""Service-layer modules — non-LLM business logic that the orchestrator
+ Celery jobs invoke. Models stay dumb-data; views stay thin; agents
stay focused on prompting; everything else lives here.
"""
