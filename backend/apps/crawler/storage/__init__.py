"""Persistence sub-package — append-only CSV streams + atomic JSON state.

Modules:
  * csv_writer   — streaming CSV writers + atomic crawl_state.json
  * repository   — read-only access to persisted CSVs for the API
  * excel_writer — beautified multi-sheet XLSX report bundler
"""
