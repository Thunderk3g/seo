"""Crawler service sub-package.

Modules:
  * crawler_service — start/stop crawl lifecycle (threaded)
  * import_service  — one-shot legacy CSV importer
  * report_service  — XLSX report generation entrypoint
"""
